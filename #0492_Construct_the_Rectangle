class Solution(object):
    def constructRectangle(self, area):
        """
        :type area: int
        :rtype: List[int]
        """
        W = int(sqrt(area)//1)
        while (area%W !=0):
            W-=1
        L = area/W
        return [L,W]
        #36%,91%
