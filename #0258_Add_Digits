class Solution(object):
    def addDigits(self, num):
        """
        :type num: int
        :rtype: int
        """
        if (num == 0):
            return 0
        else:
            return (9 if num%9 == 0 else num%9)
            #快于89%，少于33%
            #存储很大？

-----------
#return 后不加括号
