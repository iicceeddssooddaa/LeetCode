class Solution(object):
    def toLowerCase(self, s):
        """
        :type s: str
        :rtype: str
        """
        List = []
        List[:] = s
        for i in range(len(List)):
            List[i] = lower(List[i])
        String = "".join(List)
        return String
